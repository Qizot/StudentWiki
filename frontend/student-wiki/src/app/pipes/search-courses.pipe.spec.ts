import { SearchCoursesPipe } from './search-courses.pipe';

describe('SeachCoursePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCoursesPipe();
    expect(pipe).toBeTruthy();
  });
});
